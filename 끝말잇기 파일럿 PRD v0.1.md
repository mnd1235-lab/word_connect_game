# 끝말잇기 파일럿 — 제품 요구사항 정의서

버전 v0.1 (파일럿) · 2026\-09\-04 · 이전 v0.1 정식 PRD를 대체하지 않고, 그보다 앞선 검증 단계로 둔다.

## 1\. 이 파일럿이 검증하려는 것

**단 하나의 루프가 돌아가는가.**

```
사람이 단어를 입력한다 → 국어대사전 자료에 기반해 AI가 판정한다 → 상대에게 턴이 넘어간다
```

이 루프가 실제로 기분 좋게 돌아가는지만 확인한다. 확인해야 할 질문은 셋이다.

- 판정이 **믿을 만한가** — "이건 단어인데"가 몇 번이나 나오는가
- 판정이 **충분히 빠른가** — 입력하고 기다리는 시간이 게임을 끊지 않는가
- 턴이 넘어가는 감각이 **자연스러운가** — 폰을 넘겨받은 사람이 자기 차례임을 바로 아는가

여기서 답이 나오면 정식 PRD의 나머지(타이머·두음법칙·인원 확장·PWA)를 얹는다. 답이 안 나오면 얹을 것도 없다.

## 2\. 범위

| 항목 | 파일럿 | 비고 |
| --- | --- | --- |
| 같은 기기 2인 대전 | 포함 | 번갈아 입력, 순서 고정 |
| 사전 파일 기반 판정 | 포함 | 로컬 단어 목록 |
| LLM 보조 판정 | 포함 | 사전에 없을 때만 질의 |
| 판정 사유 표시 | 포함 | 왜 거부됐는지 한 줄 |
| 중복 단어 차단 | 포함 | 판 단위 |
| 브라우저 웹 페이지 | 포함 | 로컬 실행 전제 |
| **타이머** | **제외** | 시간 계산 일체 없음 |
| **두음법칙** | **제외** | 마지막 음절 그대로만 |
| **3인 이상** | **제외** | 2인 고정 |
| **한방단어 판별** | **제외** | 막히면 포기 버튼 |
| **탈락·연속 실패 카운트** | **제외** | 무효면 그냥 다시 입력 |
| **뜻풀이·신고·복구·PWA·결과 통계** | **제외** | — |

판단 기준은 하나다. **"이게 없으면 위 루프가 안 도는가."** 아니면 넣지 않는다.

## 3\. 게임 규칙

파일럿의 규칙은 네 줄이 전부다.

1. 시스템이 2음절 제시어를 하나 띄우고, 플레이어 1부터 시작한다.
2. 차례인 사람은 **앞 단어의 마지막 음절로 시작하는 명사**를 입력한다.
3. 유효하면 체인에 추가되고 **상대에게 턴이 넘어간다.** 무효면 사유가 표시되고 **차례는 그대로 유지된다.**
4. 더 이어갈 말이 없으면 '포기' 버튼을 누른다. 누른 사람이 패배하고 판이 끝난다.

### 유효 조건

|  | 조건 | 거부 문구 |
| --- | --- | --- |
| V1 | 한글 음절로만 구성 (숫자·공백·기호·자모 불가) | 한글 단어만 쓸 수 있어요 |
| V2 | 2음절 이상 | 두 글자 이상이어야 해요 |
| V3 | 앞 단어의 마지막 음절로 시작 | '{음절}'로 시작해야 해요 |
| V4 | 이번 판에 아직 쓰이지 않음 | 이미 나온 단어예요 |
| V5 | 국어대사전 기준 명사로 인정됨 | 사전에 없는 단어예요 |

검사는 **V1 → V2 → V3 → V4 → V5 순서**로 하고, 여러 조건을 어겨도 먼저 걸린 하나만 알린다. V1\~V4는 즉시 판정되고, V5만 판정에 시간이 걸릴 수 있다. 순서를 이렇게 잡는 이유가 여기 있다 — 공짜로 걸러낼 수 있는 것을 먼저 걸러 LLM 호출을 아낀다.

## 4\. 판정 엔진

### 2단 구조

```
입력 → [1] 로컬 사전 파일 조회 → 있으면 즉시 통과
                ↓ 없음
       [2] LLM에 질의 → 판정 + 한 줄 근거 → 결과를 세션 캐시에 기록
```

**1단 — 로컬 사전 파일.** 파일럿 규모는 **3,000\~5,000 어휘**로 충분하다. 전체 표제어를 넣으면 파일럿의 목적인 "루프 검증"과 무관한 데이터 작업에 시간을 다 쓴다. 명사, 2음절 이상, 한글 음절로만 구성된 것으로 자체 정제한 목록을 `data/words.txt`에 한 줄에 한 단어로 둔다. 첫 진입 시 통째로 읽어 `Set`에 올린다.

**2단 — LLM 보조.** 사전에 없는 단어만 질의한다. 파일럿 어휘가 작아서 이 경로가 자주 열리는데, 그게 의도다. 사전을 키우는 대신 LLM이 얼마나 메워 주는지를 본다.

### LLM 질의 규격

- 질문: 해당 단어가 표준국어대사전 표제어에 있는 **명사**인지. 고유명사·방언·옛말·북한어·구(句)는 인정하지 않는다.
- 응답: `{ "valid": boolean, "reason": string }` 형태의 JSON만. 자유 서술을 받지 않는다.
- `temperature: 0`, 응답 토큰 상한을 짧게 잡는다.
- 타임아웃 **3초**. 초과하면 **통과 처리**하고 화면에 "AI 확인 실패 — 통과 처리했어요"를 표시한다. 파일럿에서는 억울하게 막히는 것보다 느슨하게 통과시키는 편이 테스트를 계속하게 만든다.
- 같은 단어를 두 번 묻지 않는다. 판정 결과는 세션 동안 메모리에 캐시한다.

**API 키 취급** — 브라우저에서 LLM API를 직접 호출하면 키가 그대로 노출된다. 파일럿은 로컬 개발 서버로만 돌리므로, 키는 `.env`에 두고 **개발 서버의 프록시 경유**로만 호출한다. `VITE_` 접두사를 붙여 클라이언트 번들에 넣지 않는다. 이 상태로는 외부에 배포하지 않는다.

## 5\. 화면

화면은 **하나**다. 설정 화면도, 결과 화면도 두지 않는다.

- **상단** — 현재 차례. "플레이어 1 차례"를 화면에서 가장 크게. 턴이 바뀌면 배경색도 함께 바뀌어 폰을 넘겨받은 사람이 즉시 알아챈다.
- **중앙** — 단어 체인. 최신 단어가 가장 크고, **다음에 이어야 할 음절이 강조**된다. 지난 단어는 그 아래로 작게 쌓인다.
- **하단** — 입력창, 제출 버튼, 그 아래 작게 '포기' 버튼.
- **판정 중** — 제출 버튼이 로딩 상태로 바뀌고 입력이 잠긴다. LLM 질의가 오가는 동안 무엇이 일어나는지 보이지 않으면 앱이 멈춘 것으로 오해한다.
- **무효 시** — 입력창 아래에 사유 한 줄. 입력값은 지우지 않는다(고쳐 쓸 수 있게).
- **판 종료** — 같은 화면 위에 "플레이어 2 승리" 오버레이와 '다시 하기' 버튼.

### 입력 처리

- 턴이 넘어가면 입력창에 포커스가 자동으로 잡히고 비워진다.
- **한글 IME 조합 중에는 제출을 막는다.** `compositionstart` / `compositionend`로 조합 상태를 추적한다. 이 처리를 빼면 모바일에서 마지막 글자가 잘리거나 중복 입력된다 — 파일럿에서도 반드시 넣는다.
- `autocomplete="off"`, `autocorrect="off"`, `autocapitalize="off"`, `spellcheck=false`
- Enter 제출 지원.

## 6\. 기술 구조

> 구현 스택은 파일럿 진행 중 Streamlit + Streamlit Cloud로 변경(2026-09-04). 판정 로직·상태 정의는 그대로 유지.

- **React \+ TypeScript \+ Vite.** 단일 앱, 모노레포로 쪼개지 않는다.
- **상태는 리듀서 하나.** `(state, action) => state`. 다만 LLM 호출은 비동기라 리듀서 밖에서 처리하고, 결과만 액션으로 넣는다.
- **판정 로직은 `src/rules.ts` 한 파일로 분리.** UI를 참조하지 않는 순수 함수. 여기만 단위 테스트한다.
- **LLM 호출은 `src/judge.ts`로 분리.** 나중에 통째로 교체하거나 뺄 수 있게.
- 라우팅·상태 관리 라이브러리·UI 프레임워크를 넣지 않는다.

### 상태

```ts
type Phase = 'playing' | 'finished'

type GameState = {
  chain: { word: string; player: 1 | 2 }[]
  usedWords: Set<string>
  currentPlayer: 1 | 2
  requiredSyllable: string
  status: 'idle' | 'judging'
  lastError: string | null
  phase: Phase
  loserPlayer: 1 | 2 | null
}
```

`requiredSyllable`이 배열이 아니라 단일 문자열인 것이 정식 PRD와의 가장 큰 차이다. 두음법칙을 빼면 이어야 할 음절은 언제나 하나다.

## 7\. 완료 조건

파일럿은 아래를 모두 만족하면 끝난 것으로 본다.

- 두 사람이 한 기기로 **10턴 이상 주고받는다.**
- 로컬 사전에 있는 단어는 **체감상 즉시** 판정된다.
- 사전에 없는 단어가 LLM으로 넘어가고, **3초 안에** 결과가 나온다.
- 무효 입력 시 **사유가 하나만** 뜨고 차례가 넘어가지 않는다.
- 모바일 브라우저에서 **한글 입력이 잘리지 않는다.**

## 8\. 파일럿에서 측정할 것

숫자를 자동으로 모으지 않는다. 플레이하면서 **손으로 적는다.**

| 기록할 것 | 왜 |
| --- | --- |
| 오판정 목록 (거부됐지만 실제 단어인 것) | 사전을 키울지 LLM을 믿을지가 여기서 갈린다 |
| LLM 경로로 넘어간 비율 | 파일럿 사전 규모가 적절한지 |
| LLM 응답 시간 체감 | 게임 리듬을 깨는 지점이 어디인지 |
| 막혀서 포기한 지점 | 어휘 난이도가 맞는지 |

## 9\. 파일럿 이후

이 루프가 검증되면 정식 PRD의 항목을 하나씩 얹는다. 순서는 **타이머 → 두음법칙 → 3\~4인 → 결과 화면 → PWA**를 권한다. 앞의 둘이 판정 로직을 건드리므로 먼저 처리하고, 뒤의 셋은 판정과 무관해 나중에 붙여도 안전하다.

파일럿에서 사전 규모가 문제로 드러나면, 그때 정식 PRD 10장의 사전 파이프라인과 라이선스 확정을 착수한다. **파일럿 단계에서는 라이선스 문제가 발생하지 않는다** — 자체 정제한 소규모 목록만 쓰기 때문이다.
