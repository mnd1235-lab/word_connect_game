# 파일럿 기록지

PRD 8장의 네 가지를 **손으로 적는다.** 숫자를 자동으로 모으지 않는 것이 방침이다.
두어 판 돌리면서 채우는 이 문서가 파일럿의 실제 산출물이다.

터미널(로컬) 또는 Streamlit Cloud 앱 로그에 판정 한 건마다 아래 두 줄이 찍힌다.
경로와 소요 시간은 여기서 옮겨 적으면 된다.

```
[submit] '과일' path=local-accepted 2ms
[judge] 햇살무늬 path=llm 812ms valid=True
```

`path` 값의 뜻 — `local-accepted` 사전 통과 / `local-invalid` 규칙 위반 /
`llm` LLM 질의 / `cache` 캐시 적중 / `llm-fallback(...)` 실패해서 통과 처리.

---

## 판 기록

| 판 | 날짜 | 플레이어 | 제시어 | 총 턴 수 | 결과 | 비고 |
| --- | --- | --- | --- | --- | --- | --- |
|  |  |  |  |  |  |  |
|  |  |  |  |  |  |  |
|  |  |  |  |  |  |  |

---

## 1. 오판정 목록

거부됐지만 실제로는 쓰는 단어. **사전을 키울지 LLM을 믿을지가 여기서 갈린다.**

| 단어 | 판정 | 경로 | 어느 쪽 잘못인가 | 조치 |
| --- | --- | --- | --- | --- |
|  |  |  |  |  |
|  |  |  |  |  |
|  |  |  |  |  |
|  |  |  |  |  |
|  |  |  |  |  |

> "어느 쪽 잘못인가"는 `사전 누락`(사전에 넣으면 됨) / `LLM 오판`(LLM이 틀림) /
> `규칙`(V1~V4에서 걸림) 중에 적는다.
> 반대 방향, 즉 **말이 안 되는데 통과된 것**도 여기 적고 판정에 `통과`라고 쓴다.

---

## 2. LLM 경로로 넘어간 비율

**파일럿 사전 규모가 적절한지**를 본다.

| 판 | 총 제출 | local-accepted | local-invalid | llm | cache | LLM 비율 |
| --- | --- | --- | --- | --- | --- | --- |
|  |  |  |  |  |  |  |
|  |  |  |  |  |  |  |
|  |  |  |  |  |  |  |

> LLM 비율 = `llm / (총 제출 - local-invalid)`. 오타나 규칙 위반은 사전 규모와
> 무관하므로 분모에서 뺀다.

---

## 3. LLM 응답 시간 체감

**게임 리듬을 깨는 지점이 어디인지**를 본다. 초 단위 숫자보다 체감이 중요하다.

| 단어 | 소요(ms) | 체감 | 메모 |
| --- | --- | --- | --- |
|  |  |  |  |
|  |  |  |  |
|  |  |  |  |
|  |  |  |  |

> 체감은 `즉시` / `기다려짐` / `끊김` 셋 중에 적는다.
> 3초 타임아웃에 걸려 `llm-fallback`으로 통과된 건이 있으면 반드시 적는다.

---

## 4. 막혀서 포기한 지점

**어휘 난이도가 맞는지**를 본다.

| 판 | 몇 번째 턴 | 이어야 했던 음절 | 포기한 사람 | 사전에 후보가 있었나 | 메모 |
| --- | --- | --- | --- | --- | --- |
|  |  |  |  |  |  |
|  |  |  |  |  |  |
|  |  |  |  |  |  |

> "사전에 후보가 있었나"는 아래로 확인한다. 0이면 막다른 음절이라
> 사람 탓이 아니다.
>
> ```powershell
> python -c "import rules; s='록'; d=rules.load_dictionary('data/words.txt'); print(sum(1 for w in d if w[0]==s))"
> ```

---

## 판단

플레이를 마친 뒤 여기에 결론을 적는다. 이게 다음 단계
(타이머 → 두음법칙 → 3~4인 → 결과 화면 → PWA)의 출발점이 된다.

- **사전을 키울 것인가, LLM을 믿을 것인가** —
- **응답 시간이 문제인가** —
- **어휘 난이도가 맞는가** —
- **다음에 만들 것** —
