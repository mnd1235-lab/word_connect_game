# 끝말잇기 파일럿 — Streamlit 구현 프롬프트

PRD v0.1(파일럿)을 **Python \+ Streamlit**으로 구현하고 **Streamlit Community Cloud**에 올려, **PC 한 화면에서 두 사람이 번갈아** 플레이하기 위한 단계별 프롬프트. 위에서부터 한 단계씩 AI 코딩 도구에 붙여 넣는다.

## PRD와 달라지는 것 세 가지

PRD 파일 6장 첫머리에 한 줄 각주를 남겨 두면 나중에 혼선이 없다 — `구현 스택은 파일럿 진행 중 Streamlit + Streamlit Cloud로 변경(2026-09-04). 판정 로직·상태 정의는 그대로 유지.`

**하나. 프록시 조항이 사라진다.** PRD 4장의 "개발 서버의 프록시 경유"는 브라우저가 LLM을 직접 부르는 걸 막기 위한 장치였다. Streamlit은 파이썬이 서버에서 도니까 키가 브라우저로 갈 경로 자체가 없다. 3단계가 통째로 없어진다.

**둘. 대신 새로운 위험이 생긴다.** PRD 4장은 "이 상태로는 외부에 배포하지 않는다"고 못 박았는데, Streamlit Cloud는 기본이 **공개 URL**이다. 키가 노출되지는 않지만 **URL을 아는 누구나 앱을 열어 내 OpenAI 크레딧을 쓸 수 있다.** 그래서 8단계에서 접근 제한과 사용량 상한을 함께 건다. 이건 선택이 아니다.

**셋. 모바일 조항이 빠진다.** PC 화면으로만 하므로 PRD 5장의 `compositionstart` / `compositionend` 처리와 7장의 "모바일 브라우저에서 한글 입력이 잘리지 않는다"가 적용 대상에서 빠진다. 데스크톱 브라우저의 한글 IME는 훨씬 안전하지만 **Enter 제출만은 다르다** — 조합이 끝나지 않은 상태에서 Enter가 들어가면 마지막 글자가 잘릴 수 있다. 5단계에서 짧게 확인하고 넘어간다.

**공통 전제 — 매 단계 프롬프트 앞에 함께 붙인다.**

> 작업 폴더는 `끝말잇기`이고, 같은 폴더의 `끝말잇기 파일럿 PRD v0.1.md`가 유일한 사양서다. 단 구현 스택은 PRD 6장 대신 **Python \+ Streamlit**이고, 배포는 Streamlit Community Cloud, 플레이는 **PC 한 화면에서 두 사람이 번갈아** 한다(모바일 대응 불필요). PRD에 "제외"로 적힌 것(타이머, 두음법칙, 3인 이상, 한방단어, 탈락 카운트, 뜻풀이·PWA·통계)은 **절대 만들지 않는다.** 요청한 파일 외에는 건드리지 않는다. 작업이 끝나면 변경한 파일 목록과 확인 방법만 짧게 보고한다.

| 단계 | 산출물 | 비고 |
| --- | --- | --- |
| 0 | 파이썬 스캐폴드 | venv, requirements, secrets 구조 |
| 1 | `data/words.txt` | 3,000\~5,000 어휘 |
| 2 | `rules.py` \+ 테스트 | V1\~V4 순수 함수 |
| 3 | `judge.py` | LLM 질의, 3초, 캐시 |
| 4 | `game.py` \+ 테스트 | 상태 전이 |
| 5 | `app.py` 화면 | PC 화면 단일 레이아웃 |
| 6 | 판정 결선 | 파이프라인 연결 |
| 7 | 포기·승패·다시 하기 | 판 닫기 |
| 8 | **Cloud 배포 · 접근 제한** | 크레딧 보호가 핵심 |
| 9 | 완료 조건·기록지 | 파일럿 실행 |

* * *

## 0단계 — 파이썬 스캐폴드

로컬은 `.streamlit/secrets.toml`, 클라우드는 대시보드 Secrets를 쓴다. 처음부터 `st.secrets` 우선으로 짜 두면 배포할 때 코드를 고칠 일이 없다.

```text
현재 폴더(git 저장소, PRD와 .env, .gitignore만 있음)에 Python + Streamlit 프로젝트를
초기화해줘. 나중에 Streamlit Community Cloud에 배포할 것이므로 그 전제로 구성한다.

- Python 3.11 이상, 가상환경은 .venv
- requirements.txt: streamlit, openai, pytest
  (python-dotenv는 넣지 않는다 — 아래 secrets 방식을 쓴다)
- 파일 구조를 최소 스텁으로 미리 만들 것:
    app.py              # Streamlit 화면 (단계 5)
    rules.py            # 판정 순수 함수 (단계 2)
    judge.py            # LLM 질의 (단계 3)
    game.py             # 상태와 전이 (단계 4)
    data/words.txt      # 사전 (단계 1, 지금은 빈 파일)
    tests/__init__.py
- 비밀값 처리:
    .streamlit/secrets.toml 을 만들고 기존 .env 의 OPENAI_API_KEY 값을 옮겨 적는다.
    .streamlit/secrets.toml.example 을 만들어 `OPENAI_API_KEY = ""` 한 줄만 넣는다.
    .env 는 이제 쓰지 않으므로 삭제해도 된다(판단해서 정리하고 알려줄 것).
- .gitignore 에 추가: .venv/, __pycache__/, *.pyc, .pytest_cache/,
  .streamlit/secrets.toml
  (!.streamlit/secrets.toml.example 예외도 넣을 것. 기존 node 관련 항목은 정리해줘.)
- app.py 는 지금은 st.title("끝말잇기 파일럿") 하나만.

끝나면 가상환경 활성화부터 `streamlit run app.py` 까지의 명령을 Windows PowerShell
기준으로 순서대로 알려줘.
```

**완료 확인** — 브라우저에 제목만 뜬다. `git status`에 `secrets.toml`이 안 보인다.

```bash
git add -A && git commit -m "chore: Streamlit 프로젝트 스캐폴드와 secrets 구조"
```

* * *

## 1단계 — 사전 파일

끝말잇기는 "특정 음절로 시작하는 단어"가 있어야 이어진다. 총 단어 수보다 **시작 음절의 분포**가 게임을 살리고 죽인다. 통계 출력을 요구하는 이유가 이것이다. 배포하면 `data/words.txt`가 저장소에 있어야 클라우드에서 읽히므로 반드시 커밋한다.

```text
data/words.txt 를 만들어줘. 파일럿용 소규모 국어 명사 목록이다.

규격:
- 한 줄에 한 단어, UTF-8, 정렬, 중복 없음
- 모든 단어가 (1) 한글 음절로만 구성 (2) 2음절 이상 (3) 보통명사
- 고유명사·방언·옛말·북한어·띄어쓰기 있는 구(句) 제외
- 목표 3,000~5,000개

만드는 방법:
- scripts/build_words.py 를 작성한다. data/raw/ 아래 모든 .txt 를 읽어
  정규식 r'^[가-힣]{2,}$' 로 필터 → 중복 제거 → 정렬 → data/words.txt 로 쓴다.
- 원천 목록은 일상 어휘 중심으로 카테고리(음식, 동물, 식물, 사물, 장소, 신체,
  자연, 직업, 추상어, 행위 등)별로 data/raw/*.txt 에 직접 채운다.
  한 번에 다 쓰지 말고 카테고리당 300~500개씩 나눠 작성할 것.
- 스크립트 끝에 통계를 출력한다: 총 단어 수, 그리고 **막다른 음절** 목록 상위 30개
  (= 그 음절로 끝나는 단어는 있는데 그 음절로 시작하는 단어가 없는 음절).

그 다음 막다른 음절을 보고 해당 음절로 시작하는 단어를 원천에 보강해서,
막다른 음절이 눈에 띄게 줄 때까지 반복해줘.

data/raw/ 와 data/words.txt 둘 다 커밋한다.
```

```bash
git add -A && git commit -m "feat: 파일럿 사전 목록과 정제 스크립트"
```

* * *

## 2단계 — 판정 순수 함수 `rules.py`

**목적** — PRD 3장 V1\~V4. Streamlit을 import하지 않는다. **여기만 단위 테스트한다.**

```text
rules.py 를 구현해줘. streamlit도 openai도 import하지 않는 순수 모듈이다.

PRD 3장 유효 조건 표 그대로:
  V1 한글 음절로만 구성          → "한글 단어만 쓸 수 있어요"
  V2 2음절 이상                  → "두 글자 이상이어야 해요"
  V3 앞 단어의 마지막 음절로 시작 → "'{음절}'로 시작해야 해요"
  V4 이번 판에 아직 안 쓰임      → "이미 나온 단어예요"
  V5 사전 기준 명사              → "사전에 없는 단어예요"

    from typing import Literal, NamedTuple

    CheckKind = Literal['invalid', 'accepted', 'needs_judge']

    class LocalCheck(NamedTuple):
        kind: CheckKind
        reason: str | None = None

    def normalize(text: str) -> str
    def check_local(
        text: str, *,
        required_syllable: str,
        used_words: set[str],
        dictionary: set[str],
    ) -> LocalCheck

    def load_dictionary(path: str = 'data/words.txt') -> set[str]

규칙:
- normalize는 앞뒤 공백만 제거. 내부 공백은 남긴다(V1에서 걸러야 하므로).
- 검사 순서는 반드시 V1 → V2 → V3 → V4 → V5. 여러 조건을 어겨도 **먼저 걸린
  하나만** 반환한다. LLM 호출을 아끼는 것이 이 순서의 이유다.
- V1 정규식은 r'^[가-힣]+$'. (자모 ㄱ-ㅎ, ㅏ-ㅣ 는 이 정규식에서 자연히 걸러진다.
  IME 조합이 덜 끝난 입력을 막아 주는 안전망이기도 하다.)
- V3 문구에는 실제 요구 음절을 넣는다. 예: "'기'로 시작해야 해요"
- 두음법칙은 구현하지 않는다. 마지막 음절과 첫 음절의 정확한 일치만.
- 사전에 있으면 kind='accepted', 없으면 kind='needs_judge'.

tests/test_rules.py 를 작성한다. 최소한:
- V1: "abc", "사과1", "사 과", "ㄱㄴ", "사과ㅇ", "" → 모두 invalid
- V2: "산" → invalid
- V3: 요구 음절 불일치 → invalid, 문구에 음절이 들어갔는지
- V4: used_words에 있는 단어 → invalid
- 우선순위: V1과 V3을 동시에 위반하는 입력이 V1 문구를 반환하는지
- 사전 있음 → accepted, 없음 → needs_judge

pytest 통과까지 확인해줘.
```

```bash
git add -A && git commit -m "feat: V1~V4 로컬 판정 규칙과 단위 테스트"
```

* * *

## 3단계 — LLM 판정 `judge.py`

```text
judge.py 를 구현해줘. streamlit을 직접 import하지 않는다(캐시는 호출자가 넘기고,
키는 아래 방식으로 읽는다).

    from dataclasses import dataclass

    @dataclass
    class JudgeResult:
        valid: bool
        reason: str
        fallback: bool     # 타임아웃/실패로 통과 처리된 경우 True

    def judge_word(word: str, cache: dict[str, JudgeResult], api_key: str) -> JudgeResult

동작:
- api_key 는 호출자(app.py)가 st.secrets에서 읽어 넘긴다. judge.py 는 어디서 왔는지
  모른다. 이렇게 해야 이 모듈을 테스트하거나 나중에 다른 프론트로 옮길 수 있다.
- OpenAI Chat Completions. 모델은 gpt-4o-mini 같은 빠르고 저렴한 것.
    temperature=0, max_tokens=100, response_format={"type": "json_object"}
    클라이언트에 timeout=3.0 을 건다.
- 시스템 프롬프트 취지: "주어진 단어가 표준국어대사전 표제어에 있는 '명사'인지
  판정하라. 고유명사·방언·옛말·북한어·구(句)는 인정하지 않는다.
  {\"valid\": bool, \"reason\": str} JSON만 출력하라. reason은 한국어 한 줄."
- cache에 있으면 API를 부르지 않는다. 결과는 반드시 cache에 넣는다.
- 타임아웃·네트워크 오류·JSON 파싱 실패 → 예외를 밖으로 던지지 말고
  JudgeResult(valid=True, reason='AI 확인 실패 — 통과 처리했어요', fallback=True)
  를 반환한다. 억울하게 막는 것보다 느슨하게 통과시키는 것이 파일럿 방침이다.
  이 fallback 결과도 캐시한다.
- 소요 시간(ms)과 경로를 print로 남긴다. Streamlit Cloud에서는 이게 앱 로그로
  보이므로 파일럿 지표를 나중에 확인할 수 있다.
```

```bash
git add -A && git commit -m "feat: LLM 판정 모듈 (3초 타임아웃·캐시)"
```

* * *

## 4단계 — 상태와 전이 `game.py`

**목적** — PRD 6장의 상태를 파이썬으로 옮긴다. Streamlit의 rerun에 휘둘리지 않도록 **전이를 순수 함수로** 둔다.

```text
game.py 를 구현해줘. PRD 6장 GameState를 파이썬 dataclass로 옮긴다.

    from dataclasses import dataclass, replace
    from typing import Literal

    Phase = Literal['playing', 'finished']

    @dataclass
    class ChainItem:
        word: str
        player: int | None      # 제시어는 None

    @dataclass
    class GameState:
        chain: list[ChainItem]
        used_words: set[str]
        current_player: int          # 1 또는 2
        required_syllable: str
        last_error: str | None       # 무효 사유 (한 줄)
        notice: str | None           # "AI 확인 실패 — 통과 처리했어요" 같은 알림
        phase: Phase
        loser_player: int | None

전이는 모두 **새 GameState를 반환하는 순수 함수**로. 상태를 제자리에서 바꾸지 않는다:

    def new_game(seed_word: str) -> GameState
    def accept(state, word: str, notice: str | None = None) -> GameState
    def reject(state, reason: str) -> GameState
    def give_up(state) -> GameState
    def pick_seed_word(dictionary: set[str]) -> str

규칙:
- accept: chain에 (word, current_player) 추가, used_words에 추가,
  required_syllable을 word의 마지막 글자로, current_player를 반대편으로,
  last_error를 None으로.
- reject: 차례는 **그대로 유지**한다. last_error만 세팅.
- give_up: loser_player = current_player, phase = 'finished'.
- pick_seed_word: 2음절이고, 사전에 있고, **그 마지막 음절로 시작하는 단어가
  사전에 존재하는** 단어 중에서 무작위로 고른다. 첫 수부터 막히면 안 된다.

tests/test_game.py: accept 후 턴 교대와 required_syllable 갱신,
reject 후 턴 유지, give_up 후 loser_player가 누른 사람인지,
pick_seed_word가 막다른 단어를 고르지 않는지.
```

```bash
git add -A && git commit -m "feat: 게임 상태 전이와 테스트"
```

* * *

## 5단계 — 화면 `app.py`

**목적** — PRD 5장. 화면은 하나. **PC 모니터에서 두 사람이 나란히 보는** 화면이다.

**입력창 초기화 트릭.** PRD는 "무효 시 입력값을 지우지 않고, 턴이 넘어가면 비운다"를 요구한다. Streamlit에서는 `text_input`의 `key`에 체인 길이를 붙이면 된다 — 턴이 넘어가면 key가 바뀌어 새 위젯이 되므로 자동으로 비고, 무효면 같은 key라 값이 남는다.

```text
app.py 로 화면을 구현해줘. 화면은 하나. 이 단계에서는 표시와 입력 폼까지만 만들고
제출 처리는 다음 단계에서 붙인다. **PC 모니터를 두 사람이 함께 보는 상황**을
전제로 설계한다(모바일 대응 불필요).

기본:
- st.set_page_config(page_title="끝말잇기", layout="centered")
- 사전은 @st.cache_resource 로 한 번만 로드(rules.load_dictionary).
- 게임 상태는 st.session_state['game'] 에 GameState로. 없으면 new_game으로 생성.
- LLM 캐시는 st.session_state['judge_cache'] = {}
- API 키는 st.secrets["OPENAI_API_KEY"] 로 읽어 둔다(다음 단계에서 사용).

레이아웃:
- 상단: "플레이어 1 차례" — 화면에서 가장 큰 글자. 턴이 바뀌면 **배경색도 함께
  바뀐다**(플레이어 1과 2에 서로 다른 색). st.markdown(unsafe_allow_html=True)로
  배너 div를 그리고 색은 current_player로 정한다. 두 사람이 같은 화면을 보므로
  "지금 누구 차례인가"가 1미터 떨어져서도 보여야 한다. 폰트를 과하게 키울 것.
- 중앙: 단어 체인. 최신 단어가 가장 크고, 그 단어의 **마지막 음절(=다음에 이어야
  할 음절)을 색과 굵기로 강조**한다. 지난 단어는 그 아래로 작게 쌓인다(최신순,
  10개 정도만 보이고 나머지는 접어도 된다).
- 하단: st.form 안에 text_input + form_submit_button("제출"). 폼 밖에 작게
  '포기' 버튼.
- last_error 가 있으면 입력창 **아래**에 st.error 한 줄. notice 가 있으면 st.info 한 줄.

입력창:
- st.form(..., clear_on_submit=False) 로 두고, text_input의 key를
  f"word_{len(state.chain)}" 으로 준다. 턴이 넘어가면 key가 바뀌어 자동으로
  비워지고, 무효면 같은 key라 값이 남는다(PRD 5장 요구사항).
- st.form 안에서는 Enter로도 제출된다. 한글 IME 조합이 덜 끝난 상태에서 Enter가
  들어가면 마지막 글자가 잘릴 수 있는데, rules.py의 V1 정규식이 자모를 걸러내므로
  "한글 단어만 쓸 수 있어요"로 자연스럽게 처리된다. 별도 처리를 넣지 말 것.
- phase == 'finished' 면 입력창과 제출 버튼을 disabled로.

CSS는 파일 상단에 st.markdown으로 한 번만 주입하고 색상은 상수로 모아 둔다.

끝나면 데스크톱 Chrome에서 한글을 치고 Enter로 제출하는 걸 10회 시험해서,
마지막 글자가 잘리는 경우가 있는지 알려줘.
```

**완료 확인** — 턴 배너가 멀리서도 읽히고, 이어야 할 음절이 한눈에 보인다.

```bash
git add -A && git commit -m "feat: 단일 화면 레이아웃과 입력 폼"
```

* * *

## 6단계 — 판정 파이프라인 결선

```text
제출 처리를 붙여줘. 흐름:

  1. rules.normalize(입력)
  2. rules.check_local(...)
     - 'invalid'      → state = game.reject(state, reason). LLM 호출 없음. 즉시.
     - 'accepted'     → state = game.accept(state, word). LLM 호출 없음. 즉시.
     - 'needs_judge'  → with st.spinner("확인 중..."):
                            result = judge_word(word, cache, api_key)
                        - valid True  → accept(state, word,
                                          notice=result.reason if result.fallback else None)
                        - valid False → reject(state, '사전에 없는 단어예요')
  3. 처리 후 st.rerun() 으로 화면 갱신.

주의:
- Streamlit은 동기 실행이라 st.spinner 가 도는 동안 화면이 자동으로 잠긴다.
  별도의 'judging' 상태나 중복 제출 방지 로직이 필요 없다. 넣지 말 것.
- 무효 판정은 사유를 **하나만** 표시한다. 여러 줄을 쌓지 않는다.
- fallback으로 통과된 경우 "AI 확인 실패 — 통과 처리했어요"를 last_error가 아닌
  notice로 띄운다. 에러가 아니라 알림이다.
- 콘솔에 한 줄씩 로그: 단어, 경로(local/llm), 소요 ms.
```

```bash
git add -A && git commit -m "feat: 로컬 사전 + LLM 2단 판정 결선"
```

* * *

## 7단계 — 포기·승패·다시 하기

```text
판 종료 흐름을 붙여줘.

- '포기' 버튼: 누른 사람이 패배한다. 확인 대화상자는 두지 않는다(파일럿).
  game.give_up() → phase 'finished'.
- 종료 표시: @st.dialog 데코레이터로 모달을 띄운다. 내용은 "플레이어 2 승리"와
  '다시 하기' 버튼 하나. 화면을 갈아끼우지 않고 위에 덮는 것이 PRD 5장 요구다.
- '다시 하기': pick_seed_word 로 새 제시어를 뽑아 new_game. 체인·used_words·
  에러가 모두 비워지고 플레이어 1부터 시작한다.
  **judge_cache 는 비우지 않는다** — 같은 단어를 다시 묻지 않기 위해. 이건 판
  단위가 아니라 세션 단위 캐시다.
- 결과 통계·기록 화면은 만들지 않는다.
```

```bash
git add -A && git commit -m "feat: 포기·승패 모달·다시 하기"
```

* * *

## 8단계 — Streamlit Cloud 배포와 접근 제한

**공개 URL은 곧 공개된 지갑이다.** Streamlit Community Cloud 앱은 기본적으로 링크만 알면 누구나 연다. 키가 노출되지는 않지만, 열린 사람이 단어를 입력할 때마다 **내 OpenAI 크레딧이 나간다.** PRD 4장이 "이 상태로는 외부에 배포하지 않는다"고 적은 이유가 이것이고, 배포하기로 한 이상 그 자리를 다른 장치로 메워야 한다. 이 단계를 건너뛰지 않는다.

```text
앱을 Streamlit Community Cloud에 배포해줘. 다음 순서로 안내하고, 코드 변경이
필요한 부분은 직접 수정할 것.

1. GitHub 저장소 준비
   - 현재 로컬 git 저장소를 GitHub에 올린다. **private 저장소로 만든다.**
     (Streamlit Community Cloud는 private 저장소도 연결할 수 있다.)
   - 올리기 전에 확인할 것:
       git log --all --full-history -- .env .streamlit/secrets.toml
     결과가 비어 있어야 한다. 하나라도 걸리면 그 사실을 알리고 어떻게 지우는지
     알려줄 것.
   - data/words.txt 가 커밋돼 있는지 확인한다. 없으면 클라우드에서 앱이 죽는다.
   - requirements.txt 에 실제 import하는 패키지가 다 있는지 확인한다.

2. 배포
   - share.streamlit.io 에서 저장소를 연결하고 main file은 app.py.
   - Advanced settings에서 Python 버전을 로컬과 맞춘다.
   - Secrets 칸에 다음을 붙여 넣는다:
       OPENAI_API_KEY = "..."
       APP_PASSWORD = "..."     # 아래 3번에서 쓴다

3. 접근 제한 — 아래 둘을 **모두** 적용한다.
   a. Streamlit Cloud 앱 설정에서 뷰어를 제한한다(Settings → Sharing →
      특정 이메일만 보기 가능). 이게 1차 방어선이다.
   b. 코드에도 간단한 비밀번호 게이트를 넣는다. app.py 맨 위에서
      st.session_state에 인증 플래그가 없으면 비밀번호 입력창만 보여 주고
      st.stop() 한다. 입력값을 st.secrets["APP_PASSWORD"] 와 비교한다.
      (틀린 횟수 제한 같은 건 넣지 않는다 — 파일럿이다.)

4. 사용량 상한
   - OpenAI 대시보드에서 이 키에 월 사용 한도(usage limit)를 낮게 걸어 두라고
     안내해줘. 구체적인 메뉴 경로까지.

5. 배포 확인
   - 앱 URL로 접속해 비밀번호 게이트가 뜨는지, 통과 후 게임이 도는지,
     사전에 없는 단어가 LLM으로 넘어가는지 확인한다.
   - Streamlit Cloud는 일정 시간 접속이 없으면 앱이 잠든다. 다시 깨울 때
     첫 화면이 뜨기까지 시간이 걸린다는 점을 README에 적어 둘 것.
```

**완료 확인** — 다른 기기에서 URL을 열면 비밀번호를 묻는다. 저장소 히스토리에 키가 없다.

```bash
git add -A && git commit -m "feat: Cloud 배포 설정과 비밀번호 게이트"
```

* * *

## 9단계 — 완료 조건과 기록지

**목적** — PRD 7장·8장. 코드를 더 쓰는 단계가 아니라 **파일럿을 실제로 돌리는** 단계다.

```text
PRD 7장 완료 조건을 점검하고 결과를 보고해줘. 수정이 필요하면 최소한으로만.

  [ ] 두 사람이 한 화면으로 10턴 이상 주고받는다
  [ ] 로컬 사전에 있는 단어는 체감상 즉시 판정된다
  [ ] 사전에 없는 단어가 LLM으로 넘어가고 3초 안에 결과가 나온다
  [ ] 무효 입력 시 사유가 하나만 뜨고 차례가 넘어가지 않는다
  [ ] (모바일 조항은 적용 대상 아님 — PC 화면 전용으로 진행)

추가로:
- README.md: 로컬 실행(.venv, pip install, secrets.toml, streamlit run)과
  배포 앱 URL, 그리고 잠든 앱이 깨는 데 시간이 걸린다는 안내.
- PILOT-LOG.md 를 만들어 PRD 8장의 4개 항목(오판정 목록 / LLM 경로 비율 /
  응답 시간 체감 / 막혀서 포기한 지점)을 손으로 적을 빈 표로 준비해줘.
```

**여기서부터는 코드가 아니라 플레이다.** PRD 8장은 숫자를 자동으로 모으지 말고 손으로 적으라고 한다. 두어 판을 돌리며 `PILOT-LOG.md`를 채우는 것이 이 파일럿의 실제 산출물이다. 오판정이 많으면 사전을 키우고, 적으면 LLM을 믿는다 — 그 판단이 다음 단계(타이머 → 두음법칙 → 3\~4인 → 결과 화면 → PWA)의 출발점이 된다.

```bash
git add -A && git commit -m "docs: README와 파일럿 기록지, 완료 조건 점검"
```

* * *

## 막혔을 때 쓰는 보조 프롬프트

| 상황 | 붙여 넣을 프롬프트 |
| --- | --- |
| 사전에 없다고 자꾸 거부됨 | `앱 로그에서 LLM 경로로 넘어간 단어와 판정 결과를 표로 정리해줘. 오판정으로 보이는 것만 data/raw/에 추가하고 build_words.py를 다시 돌린 뒤 재배포해줘.` |
| 클라우드에서만 앱이 죽음 | `Streamlit Cloud 로그를 보고 원인을 찾아줘. 로컬에는 있고 저장소에는 없는 파일(data/words.txt, secrets 키), requirements.txt 누락, 파이썬 버전 차이를 먼저 의심할 것.` |
| 상태가 자꾸 초기화됨 | `st.session_state 사용처를 훑고, rerun 사이에 유지돼야 할 값이 위젯 key와 충돌하는 곳이 있는지 확인해줘. 특히 사전과 judge_cache가 매번 다시 만들어지지 않는지.` |
| 판정이 느림 | `판정 소요 시간을 local/llm 경로별로 나눠 측정하고 3초 타임아웃에 걸리는 비율을 알려줘. 더 빠른 모델로 바꿨을 때의 차이도 비교해줘.` |
| 크레딧이 예상보다 빨리 닳음 | `judge_cache 적중률을 로그로 측정해줘. 그리고 세션이 바뀔 때마다 캐시가 날아가는 문제를 @st.cache_data 로 앱 전체 캐시로 바꾸는 방안의 장단점을 알려줘.` |
| 범위가 늘어나는 느낌 | `지금 코드에 PRD "범위" 표에서 제외로 표시된 기능(타이머·두음법칙·3인 이상·한방단어·탈락 카운트·통계)의 흔적이 있는지 훑고, 있으면 제거 목록을 보여줘.` |
